{
  "name": "@rag/db",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo \"(placeholder) db build\"",
    "lint": "echo \"(placeholder) db lint\"",
    "typecheck": "echo \"(placeholder) db typecheck\"",
    "clean": "rm -rf dist .turbo"
  }
}
